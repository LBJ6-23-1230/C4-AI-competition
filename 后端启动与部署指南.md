# 后端启动与部署指南

> **知学Mate** · Python Agent 后端
> 2026 中国高校计算机大赛 · 人工智能创意赛 · 鸿蒙赛道 ｜ 参赛队伍：暗影骑士王们

---

## 一、这个后端是什么

**单进程同时提供两层能力**，前端只需要配**一个 baseUrl**：

| 层 | 端点 | 职责 |
|---|---|---|
| **自然语言层** | `POST /api/agent/chat` | 大模型意图识别 + 多模态识图 + 6 路子 Agent |
| **结构化工作流层** | `/api/v1/**` | Agent 循环 + **确定性**判分 / 掌握度 / 优先级 / 重规划 |
| 运维探针 | `GET /api/agent/health`、`GET /health`、`GET /` | 连通性、LLM 就绪状态、服务自述 |

> **为什么合并成一个进程**：项目早期是"聊天打 B1 后端、结构化流打 B2 后端"的双后端结构，
> 前端要配两个地址、契约容易漂移。现在收敛为单一真源，前后端契约
> `contracts/openapi.json` 是唯一依据。

### 设计纪律（评审可重点看这一条）

> **大模型只负责「理解意图」与「生成自然语言」；
> 所有业务计算 —— 判分、掌握度、优先级、重规划 —— 一律留在确定性代码里。**

因此同一份输入**永远得到同一个分数**，不受模型随机性影响。
可用固定演示数据复现：

```
错 √√× → score 66.67        全错 → score 0.0
掌握度   42 → 58             计划任务时长 [30,30] → [45,15]
profileVersion [1,2]         planVersion [1,2]
```

---

## 二、环境要求

| 项 | 要求 | 说明 |
|---|---|---|
| Python | **3.12+** | 开发与验证使用 3.12.13 |
| 依赖 | 见 `requirements.txt` | flask / flask-cors / openai / python-dotenv / pytest |
| 网络 | 可选 | **不联网也能跑**（见 §三） |
| 端口 | 默认 **5000** | 可用 `--port` 改 |
| 磁盘写入 | `data/` 与 `chat/` | 运行期状态，首启自动重建 |

安装依赖：

```bash
pip install -r requirements.txt
```

---

## 三、大模型 Key：不填也能跑（诚实降级）

后端**不把"没有 Key"伪装成"模型在工作"**：

| 情况 | 行为 |
|---|---|
| **配置了 `DASHSCOPE_API_KEY`** | 走真实大模型（意图识别、识图、自然语言回复） |
| **未配置 / 调用失败** | 聊天层降级为**本地确定性规则**，回复文案明确标注「本次大模型调用失败，已降级」或「未接入大模型」；响应的 `llmUsed=false` 供前端显示来源标签 |

> 这一点是刻意的：**宁可显示"我是规则兜底"，也不假装是 AI 生成。**
> 前端「接口环境」页与本文件都据此区分真模型与降级。

### 为什么要用 `qwen-vl-max` 而不是 `qwen-vl-plus`

`.env.example` 的默认值已设为 `qwen-vl-max`。原因：本项目**实测**当前账号下
`qwen-vl-plus` 返回 `403 AllocationQuota.FreeTierOnly`（免费额度不可用），
照旧默认值会在**第一次识图请求就失败**。

可用 `tools/check_llm_ready.ps1` 做逐模型自检（会真调用，能查出额度耗尽 / 欠费 / Key 失效）。

---

## 四、启动方式（按场景选）

### 方式一 · 开发工作区（最快）

虚拟环境与 `.env` 均已就绪：

```powershell
cd <工程根>\server\zhixue-agent-server
<工程根>\..\.venv-lt\Scripts\python.exe run.py
```

### 方式二 · 提交版 / clone 后（评审与队友用这个）

仓库**故意不含 `.env` 与 `.venv`**（密钥与虚拟环境不应进版本库），因此两步准备：

```powershell
cd server/zhixue-agent-server

# 1) 建虚拟环境并装依赖
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt

# 2) 准备配置（Key 可选）
Copy-Item .env.example .env
#    如需真实大模型，编辑 .env 填入 DASHSCOPE_API_KEY=sk-xxxx

# 3) 启动
.\.venv\Scripts\python.exe run.py
```

> Linux / macOS 把 `.\.venv\Scripts\` 换成 `.venv/bin/` 即可。

### 方式三 · 只做自检，不启动服务

```bash
python run.py --check
```

打印配置横幅后立即退出，用于确认"环境是否就绪"而不占用端口。

---

## 五、启动成功的标志

启动后会打印一段横幅，**必须看到这两行**：

```
  契约版本   : api-contract-v0.3
  监听地址   : http://0.0.0.0:5000
  聊天层     : POST /api/agent/chat   (GET /api/agent/health 探针)
  工作流层   : /api/v1/demo|workflows|profile|plans|exercises|traces|...
  LLM        : 已配置 DASHSCOPE_API_KEY，走真实大模型
  API Key 头 : X-API-Contract-Version: api-contract-v0.3
 * Running on all addresses (0.0.0.0)
```

| 检查点 | 期望 | 不对怎么办 |
|---|---|---|
| `监听地址` 是 `0.0.0.0` | ✅ | 只监听 127.0.0.1 时模拟器/真机连不上 |
| `LLM` 行 | 已配置 / 未接入 | 显示"未接入"时聊天会是规则兜底，不是故障 |
| `* Running on ...` | ✅ | 没有则说明端口被占用（见 §九） |

### 验证连通

```powershell
# 健康探针
curl http://127.0.0.1:5000/api/agent/health
```

期望：

```json
{"status":"ok","llm_ready":true,"model":"qwen-vl-max",
 "provider":"qwen","contractVersion":"api-contract-v0.3"}
```

### 一键全量验证（推荐）

项目自带联调脚本，会自己起服务、跑 **95 项 HTTP 检查 + 327 项单元测试**并归档：

```bash
python integration/run_liantiao5.py --port 5300
```

期望结尾：

```
  结果：95/95 通过（通过率 100.0%）
  score（√√×）        : 66.67  期望 66.67
  mastery             : 42 → 58  期望 42 → 58
  契约 PASS | 后端测试 PASS | 启动 PASS | 联调 PASS
```

---

## 六、命令行参数

```bash
python run.py                     # 默认 5000 端口
python run.py --port 5300         # 换端口（5000 被占用时用）
python run.py --check             # 只做配置自检，不启动服务
python run.py --quiet             # 关闭逐请求访问日志（联调脚本内部使用）
```

---

## 七、目录结构

```
server/zhixue-agent-server/
├── run.py                 入口（banner + 参数解析 + 启服务）
├── requirements.txt       依赖清单
├── .env.example           环境变量模板（复制为 .env）
├── app/
│   ├── __init__.py        create_app()、契约版本闸门、可选鉴权、统一错误体
│   ├── api/               路由层（chat / workflows / exercises / plans / profile /
│   │                      auth / knowledge / traces / experiments / demo …）
│   ├── agent/             对话层与专项 Agent（chat_llm / knowledge / proactive /
│   │                      partner_match …）
│   ├── agents/            工作流内的五个 Agent（secretary / diagnosis / exercise /
│   │                      planner / assessment）
│   ├── decision/          **确定性决策**：priority（五因子）/ replan_rules
│   ├── tools/             **确定性工具**：assessment_tools（判分+掌握度）/
│   │                      exercise_tools / plan_tools / trace_tools
│   ├── domain/            领域模型
│   ├── repositories/      持久化边界（JsonRepository / SqliteRepository）
│   ├── runtime/           Agent 循环编排（loop / orchestrator / session）
│   └── model_adapters/    大模型适配（Qwen，OpenAI 兼容协议）
├── chat/                  对话历史 + 提示词 + 演示数据
│   ├── prompts/           6 个子 Agent 的提示词
│   ├── mock_users.json    演示用户
│   ├── mock_courses.json  演示课程/任务
│   └── mock_wrong_questions.json
└── data/
    ├── question_bank.json 题库（**内容数据，随包提供**）
    └── repository.json    运行期状态（首启自动生成，不入库）
```

---

## 八、环境变量

| 变量 | 默认值 | 说明 |
|---|---|---|
| `DASHSCOPE_API_KEY` | （空） | 阿里云百炼 / DashScope Key。留空则诚实降级 |
| `LLM_BASE_URL` | `https://dashscope.aliyuncs.com/compatible-mode/v1` | OpenAI 兼容端点 |
| `LLM_MODEL` | `qwen-vl-max` | 文本意图与回复 |
| `LLM_VL_MODEL` | `qwen-vl-max` | 图片识别 |
| `LLM_DECISION_MODEL` | `qwen-plus` | 工作流结构化决策（纯文本，省 VL 配额） |
| `LLM_TIMEOUT_SECONDS` | `30` | 单次调用超时 |
| `EMBED_MODEL` | `text-embedding-v3` | 知识库语义检索 |
| `PORT` | `5000` | 监听端口（也可用 `--port`） |
| `ZHIXUE_ENV` | `development` | 开发模式下验证码接口返回 `devCode`；生产模式拒绝暴露 |
| `ZHIXUE_DB` | （空 = JSON） | 设 `sqlite` 改用并发安全的 SQLite 后端 |

> **两条 LLM 路径分开是刻意设计**：识图需要 VL 模型，工作流决策用更便宜的
> 纯文本模型，避免占用 VL 配额。不要为了"统一"改成同一个模型。

---

## 九、常见问题

| 现象 | 原因 | 解决 |
|---|---|---|
| `端口 5000 被占用` | 已有实例在跑 | `python run.py --port 5300`，或先关掉旧实例 |
| App 显示**「连接超时或网络不可用」** | **后端没启动**（最常见） | 起后端；确认 `监听地址` 是 `0.0.0.0` |
| 模拟器连不上 | 地址填成了 `127.0.0.1` | 模拟器必须用 `http://10.0.2.2:5000` |
| 真机连不上 | 用了模拟器地址，或不在同一 WiFi | 改为 `http://<开发机局域网IP>:5000`；放行防火墙 5000 端口 |
| 聊天回复带「已降级为本地确定性规则」 | 未配 Key 或调用失败 | 正常行为（诚实标注）。要真实模型请配 `DASHSCOPE_API_KEY` |
| 识图报 403 | 用了 `qwen-vl-plus` | 改用 `qwen-vl-max`（`qwen-vl-plus` 实测免费额度不可用） |
| 契约版本 409 | 请求头版本不匹配 | 带 `X-API-Contract-Version: api-contract-v0.3`；不带头则放行 |
| 冷启动后数据"没了" | `data/repository.json` 是运行期状态 | 用 `POST /api/v1/demo/reset` 重建演示数据 |

---

## 十、接口一览（速查）

### 自然语言层

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/agent/chat` | 统一对话入口（文本 / 图片） |
| GET | `/api/agent/health` | 连通性 + LLM 就绪状态 |
| GET / DELETE | `/api/agent/history` | 读取 / 清空当前身份对话历史 |
| GET | `/api/agent/user-data` | 一次性拉取用户/课程/候选人/错题 |

### 结构化工作流层

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/v1/demo/reset` | 重置为固定演示基线 |
| POST | `/api/v1/workflows` | 创建诊断工作流（`autoRun` 可一键跑通） |
| GET | `/api/v1/workflows/{id}` | 工作流状态 |
| POST | `/api/v1/workflows/{id}/run` | 推进工作流 |
| GET | `/api/v1/profile/{userId}` | 学习画像（含掌握度） |
| GET | `/api/v1/plans/current` | 当前计划 + **五因子拆解**与理由 |
| GET | `/api/v1/plans/{id}/diff` | 计划版本差异 |
| GET | `/api/v1/exercises/{setId}` | 拉取题集（**已剥离答案**） |
| POST | `/api/v1/exercises/{setId}/submit` | 提交答案 → 判分 + 掌握度 + 重规划 |
| GET | `/api/v1/traces/{traceId}` | Agent 决策轨迹（agent / tool 调用链） |
| POST | `/api/v1/agent/proactive` | 主动提醒决策 |
| POST | `/api/v1/agent/partner-match` | 学习搭子匹配（**确定性算分**） |
| GET | `/api/v1/experiments/snapshot` | 匿名实验数据快照 |

### 鉴权

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/v1/auth/register` | 手机号**必填**；重复号码返回 409 |
| POST | `/api/v1/auth/login` | 昵称免密登录（演示/单机场景） |
| POST | `/api/v1/auth/send-code` | 手机验证码签发 |
| POST | `/api/v1/auth/verify-code` | 验证码校验（200 已存在 / 201 新建） |
| POST | `/api/v1/auth/login-with-huawei` | 华为账号一键登录（客户端传 `openId`） |
| POST | `/api/v1/auth/logout`、GET `/auth/me`、DELETE `/auth/account` | 登出 / 当前用户 / 注销 |

> ⚠️ `login-with-huawei` **不校验 OpenID 真伪**（那需要服务端调华为接口验签）。
> 演示与单机场景可用；**生产环境必须补上验签**，否则伪造 `openId` 即可登入他人账号。

---

## 十一、测试

```bash
# 单元测试（320 项）
python -m pytest -q

# 全量联调（契约闸门 + 单测 + 95 项 HTTP + 归档）
python ../../integration/run_liantiao5.py --port 5300
```

配套检查脚本（在 `tools/`）：

| 脚本 | 检查什么 |
|---|---|
| `diff_contracts.py` | 契约真源与镜像是否逐字节一致 |
| `verify_contract_declarations.py` | 契约声明与实际行为是否一致（21 项） |
| `check_frontend_validator_coverage.py` | 联调闸门是否覆盖前端所有实际调用 |
| `check_runtime_data.py` | 交付物里是否混入运行态数据 |
| `check_llm_ready.ps1` | 逐模型验证大模型可用性（额度/欠费/Key） |

---

## 十二、已实测的验证状态

| 项 | 结果 |
|---|---|
| 后端单元测试 | **327 passed** |
| 全量接口联调 | **95 / 95（100%）** |
| 契约一致性 | 真源与镜像**逐字节一致**（109,269 B，同 sha256） |
| 演示基线 | **零漂移**（66.67 / 全错 0.0 / 42→58 / [30,30]→[45,15]） |
| 并发保护 | 四处「读—改—写」序列已加锁（对话历史 / 提交幂等 / trace / 工作流推进） |
| 交付包洁净 | 无 `.env`、无证书、无运行态数据 |

---

*本文件由项目维护，路径与命令均取自实测可运行状态。*
