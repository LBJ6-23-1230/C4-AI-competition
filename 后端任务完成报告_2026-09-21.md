# liantiao4 后端任务完成报告

日期：2026-09-21

项目目录：`C:\Users\86150\Desktop\liantiao4-backend`

## 已完成

- 已读取并配置本机 `.env` 中的 `DASHSCOPE_API_KEY`，服务自检识别为已配置。
- 实测发现 `qwen-vl-plus` 免费额度耗尽，接口返回
  `AllocationQuota.FreeTierOnly`；本机已切换为可用的 `qwen-vl-max`。
- 新增独立的 `LLM_DECISION_MODEL`，工作流决策层使用 `qwen-plus`，与聊天和识图层
  `LLM_MODEL` / `LLM_VL_MODEL` 明确分工。
- 更新 `.env.example` 和 README，写明模型分工与 JSON / SQLite 部署选择。
- 完整后端测试：`282/282` 通过。
- 首轮完整 live 联调：`95/95` 通过，退出码 0。
- 连续 10 轮 live 联调：每轮 `95/95` 通过，退出码 0，证据均已写入 `evidence/`。
- 11 轮 live 证据均验证 6 项演示基线零漂移。
- 连续 10 轮的 `stateVersion` 轨迹均为 `[2,2,2,2,2]`，幂等性验证通过。
- 题集检查未泄漏 `answerKey`，trace 合规检查为 0 违规。
- 交付包 `data/repository.json` 保持干净基线：`38,418 B`、1 个用户、1 个会话。

## 量化快照

已从干净主链场景导出：

- 覆盖 agent：3 个
  - `assessment`
  - `exercise`
  - `secretary`
- 覆盖 tool：3 个
  - `grade_exercise`
  - `select_exercises`
  - `update_mastery`
- 平均掌握度提升：`+16`
- trace 合规违规：`0`
- `replanCount`：`1`
- `replanDenominator`：`1`
- `replanRate`：`1`
- `replanRateBasis`：`plan_histories/submissions`

快照 JSON：`experiments_snapshot_2026-09-21_live.json`

## 验证命令

```powershell
cd C:\Users\86150\Desktop\liantiao4-backend\server\zhixue-agent-server
& C:\Users\86150\Desktop\liantiao4-backend\.venv\Scripts\python.exe -m pytest tests\ -q

cd C:\Users\86150\Desktop\liantiao4-backend
& C:\Users\86150\Desktop\liantiao4-backend\.venv\Scripts\python.exe integration\run_liantiao4.py --port 5097 --llm-mode live
```

## 残余说明

纯后端交付包不含前端镜像 `app/contracts/openapi.json`。单独运行
`tools/sync_contract.py --check` 会因此返回 1；一键联调脚本已专门识别这种打包形态，
契约闸门正常通过。按任务书硬规则未修改 `tools/`。
