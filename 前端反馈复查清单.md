# 前端反馈问题 · 复查清单

> 前端同学反馈的每一项，逐条复查并给出**当前状态 + 验证方式**。
> 复查时间：2026-09-24 ｜ 依据：`main` / `main2` 最新代码

---

## 汇总

| # | 反馈 | 状态 | 验证方式 |
|---|---|---|---|
| 1 | 知识库进不去、报契约不一致 | ✅ **已修** | 登录后实测 200 |
| 2 | 知识库建不了（输「数据结构+111」等都不行） | ✅ **已修**（同一根因） | 实测 201 |
| 3 | 搭子页「联机模式暂不生成搭子结果」 | ✅ **已接入后端算分** | 实测 90 / 60 分 |
| 4 | 「接口环境」页该删 | ⚪ **建议保留**（有据） | 见第四节 |
| 5 | 登录注册真的能用吗 | ✅ **能用** | 22 项闸门全通过 |
| 6 | 数据库在哪能看 | ✅ **已说明** | `data/repository.json` |
| 7 | 题目一直是那几个 | ⚪ **设计使然**（非 bug） | 见第五节 |
| 8 | 「AI 的测试根本不能相信」 | ✅ **已用证据回应** | 327 单测 + 95/95 |
| 9 | 我的页没有头像昵称 | ✅ **已补头像** | 本次新增 |
| 10 | 「不管错误率是多少都是 45/15 分钟」 | ✅ **已修** | 现在随得分变化 |

**结论：10 项里 8 项已解决、2 项为设计使然并已说明。**

---

## 一、账号登录相关（你最关心的）

### 1.1 根因回顾

她报的原话是「游客可以，账户登录就出现了这种 bug」——
**这个描述精确命中了根因**：

`AgentApiClient.listKnowledgeBases()` 在登录后会拼上 userId：

```
/api/v1/knowledge-bases?userId=xxx
```

而 `ApiResponseValidator` 当时对路径用**精确比较**：

```ets
if (path === '/api/v1/knowledge-bases') {   // 带 ?userId= 时不成立
```

带查询串 → 该分支不成立 → **掉进「详情」分支**去要 `kbId`/`name`
→ 列表响应没有这两个字段 → 校验失败 → 报「数据结构不一致」。

| 身份 | 带 userId | 结果 |
|---|---|---|
| 游客 / demo-user | 否 | ✅ 正常 |
| **登录真实账号** | 是 | ❌ 报错 |

### 1.2 修复内容

`ApiResponseValidator` 新增 `basePath()`，进 `isValid` 即剥离查询串，
类内**全部 33 处**路径匹配由 `path` 改为 `route`。

### 1.3 本次复查做的验证（3 道闸门，全部 PASS）

#### 闸门 A：校验器覆盖审计（新增 `tools/audit_validator_coverage.py`）

枚举前端**全部 27 个**接口调用，逐条判断校验器能否命中：

```
结论：PASS —— 前端调用的每个接口都有对应校验器分支
```

其中标出「登录后」才走的 10 个（这些最容易漏测，因为游客路径不经过）：
`authMe` / `authLogout` / `authDeleteAccount` / `authVerifyCode` /
`authLoginWithHuawei` / `createKnowledgeBase` / `getKnowledgeBase` /
`deleteKnowledgeBase` / `uploadKnowledgeDocument` / `searchKnowledgeBase`

#### 闸门 B：登录后全链路实测（新增 `tools/check_logged_in_flows.py`）

注册一个**真实账号**，带 token 走一遍所有登录后接口 ——
**知识库列表刻意按 App 的写法带 `?userId=`**（这正是当初出问题的形态）：

```
总计 16 项，通过 16，失败 0
结论：PASS —— 登录后功能全部可用
```

覆盖：注册 → `/auth/me` → 知识库列表（带 `?userId=`）/新建/详情/文档列表/
检索（7 个必需字段）/上传文档 → 学习画像（`profile.userId`）/当前计划/
题集/提交练习/搭子匹配 → **越权检查**（用自己 token 带别人 userId 查不到别人数据）

#### 闸门 C：账户链 22 项（`tools/check_auth_chain.py`）

```
JSON   后端（5000）：22 项通过 22，失败 0
SQLite 后端（5500）：22 项通过 22，失败 0
```

### 1.4 两个后端都验过

| 后端 | 登录后全链路 | 账户链 |
|---|---|---|
| JSON（默认） | **16/16** | **22/22** |
| SQLite（`ZHIXUE_DB=sqlite`） | **16/16** | **22/22** |

---

## 二、我的页头像昵称（本次新增修复）

### 复查结论：她说得对，确实是缺的

| 项 | 复查前 | 复查后 |
|---|---|---|
| 昵称 | ✅ 已渲染（她截图里的 `1` 是她注册时填的昵称，不是 bug） | ✅ 保留 |
| **头像** | ❌ **完全没有** | ✅ 新增（昵称首字符圆形色块） |
| **脱敏手机号** | ❌ 未显示 | ✅ 新增 |
| **注册时间** | ❌ 未显示 | ✅ 新增 |

### 为什么之前没显示手机号

`appState.currentUser` 是 `UserProfile`（学习画像用的 `basicInfo` 结构），
**里面没有** `phoneMasked` / `createdAt`。而 `/api/v1/auth/me` 是有的：

```
/auth/me 的 user 字段:
  authProvider, createdAt, grade, hasPhone, lastLoginAt,
  nickname, phoneMasked, userId
```

所以「我的」页现在会额外拉一次 `/auth/me` 补齐这几项。
拉取失败时**不报错也不清空** —— 本地已有昵称/年级，先显示能显示的。

---

## 三、重规划时长不随错误率变化（本次新增修复）

她实测反馈："不管结果错误率是什么，都是 45 / 15 分钟。"

**实测确认属实**，且根因是重规划**完全没读 `assessmentScore`**：

| 正确率 | 得分 | 修复前 | 修复后 |
|---|---|---|---|
| 全对 | 100 | 未触发 | 未触发 |
| 对2错1 | 66.67 | [45, 15] | **[45, 15]**（基线保持） |
| 对1错2 | 33.33 | [45, 15] | **[50, 15]** |
| 全错 | 0 | [45, 15] | **[55, 15]** |

现在**得分越低，薄弱点任务分到越多**。详见
`项目文档` 与提交信息（`fix(replan)` 那条）。

---

## 四、「接口环境」页该不该删

她的判断在产品视角下是对的 —— 那确实是内部联调工具。

**但本作品有三条需求依赖它**：

| 依赖 | 说明 |
|---|---|
| 录制演示视频 | 需要现场切离线/在线，展示"诚实降级"这个技术点 |
| **评委自己装了 HAP** | 得自己填后端地址（模拟器 `10.0.2.2` / 真机局域网 IP） |
| 答辩 | "配置透明"是可以主动讲的一点 |

**删了评委就配不了地址。** 折中方案：把入口从首页挪进「我的 → 设置」。

> 结论：**建议保留**，是否挪入口由队长定。

---

## 五、题目固定 —— 设计使然，不是 bug

她的疑问是"是不是没上传 DDL 导致的"。**不是。**

* `/api/v1/exercises/**` **完全不读** DDL（对 `user_data` / `courses` 零引用）
* 出题由**确定性代码**完成（`select_exercises`），**不走大模型** —— 这是刻意设计
* 固定 3 题是为了让演示基线（66.67 / 42→58）**可复现**
* 题库其实有 **30 题 / 6 个知识点**，带 `?knowledgePointId=` 参数可以换题，
  只是 App 界面没暴露入口

完整说明见 `出题机制与DDL上传说明.md`。

---

## 六、关于「AI 的测试根本不能相信」

这句分两半看：

**"AI 写的代码不能信"** —— 同意，所以每个结论都留证据：
327 项单元测试、95/95 接口联调、契约逐字节一致闸门、
校验器覆盖审计、查询串闸门、账号链闸门、登录后链路闸门。

**她这次的反馈就是最有效的验证**：10 项里有 3 项是真问题
（知识库契约、重规划时长、我的页头像），都已修复。

**"AI 出的题不能信"** —— 这也正是项目**刻意不让模型出题**的原因：
出题与判分全部由确定性代码完成，同一输入永远同一结果。

---

## 七、给她的回复要点（可直接复制）

> 你报的问题我逐条查了，**3 个是真 bug，都已修**：
>
> 1. **知识库进不去 / 建不了** —— 根因是你发现的规律："游客可以、账户登录就出问题"。
>    `listKnowledgeBases` 在登录后会拼 `?userId=`，而前端校验器当时对路径做**精确比较**，
>    带参数后分支失配 → 走进"详情"分支 → 报契约不一致。
>    已改为**先剥掉查询串再匹配**，类内 33 处统一改。**现在登录后知识库功能正常。**
>
> 2. **搭子页"暂不支持"** —— 后端一直有接口，是前端没接。已接入真实五因子算分，
>    现在联机模式能看到 🏅 小红 **90 分** / 小刚 **60 分**，含逐因子拆解。
>
> 3. **"不管错多少都是 45/15 分钟"** —— 你是对的。重规划**完全没读得分**，
>    时长改动是写死的 ±15。现在改为按得分分级：66.67→[45,15]、33.33→[50,15]、0→[55,15]。
>
> 另外「我的」页**头像确实没有**（你说得对），已补：
> 昵称首字符圆形头像 + 脱敏手机号 + 注册时间。
> 昵称显示 `1` 不是 bug —— 那是你注册时填的昵称。
>
> 还有一个澄清：**「接口环境」页建议保留**。评委装了你交的 HAP 后要自己填后端地址，
> 删了就配不了。可以把它从首页挪进「我的」里，降低误入。
>
> 补了三道自动闸门防止这类问题再出现（账号链 22 项 / 登录后链路 16 项 /
> 校验器覆盖 27 个接口），两个后端（JSON 与 SQLite）都跑通。

---

*本文所有结论均实测核对；闸门脚本在 `tools/` 下，可随时复跑。*
