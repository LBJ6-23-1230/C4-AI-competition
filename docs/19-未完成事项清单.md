# liantiao4 · 未完成事项清单

> **这份清单是什么**：本轮"把技术改进需求全部落地"过程中，**无法在当前环境
> 验证或完成**的事项。
>
> **为什么单独列出来**：本环境**没有 DevEco Studio**（不能编译 ArkTS）、
> **没有真机**、**没有 `DASHSCOPE_API_KEY`**。凡依赖这三样的事情，
> 我只能把代码写完、把符号核验到位，**但无法证明它能跑**。
> 把它们和"已验证"的改进混在一起讲，就是在夸大成果。
>
> **配套**：[`18-技术改进实施报告.md`](18-技术改进实施报告.md)（已完成部分） ·
> [`17-前端同学任务书.md`](17-前端同学任务书.md) · [`02-下一步工作清单.md`](02-下一步工作清单.md)

---

## 零、汇总表

| # | 事项 | 为什么本环境做不了 | 阻塞级别 | 预估 | 归属 |
|---|---|---|---|---|---|
| A1 | **ArkTS 编译全部前端改动** | 无 DevEco Studio | 🔴 阻塞交付 | 1h | 前端 |
| A2 | **签名 → 真机安装 → 走主链** | 无真机 + 无签名证书 | 🔴 阻塞答辩 | 半天 | 前端 |
| A3 | **`TokenCipher` 两处加密约定实测** | 需真机/模拟器 | 🔴 会使登录态失效 | 1h | 前端 |
| A4 | **验证码登录端到端（真机）** | 需真机 + 后端 | 🟠 | 2h | 前端 |
| A5 | **14 个前端单测实际运行** | 需 DevEco 测试框架 | 🟠 | 1h | 前端 |
| A6 | **意图框架 `hasInsightIntent` 产物验证** | 需编译 | 🟠 | 1h | 前端 |
| A7 | **`DASHSCOPE_API_KEY` 配置** | 无 Key 且不该由我写死 | 🔴 阻塞 AI 演示 | 30min | 后端 |
| A8 | **语义检索真实推理效果** | 需 Key | 🟠 | 1h | 后端 |
| A9 | **`@ohos.router` → `Navigation` 迁移（61 处）** | 无法编译验证的结构性改动 | 🟡 | 2 天 | 前端 |
| A10 | **其余 18 个页面的响应式落地** | 同上 | 🟡 | 2 天 | 前端 |
| A11 | **`414` 真实触发验证** | 测试客户端不施加请求行长度限制 | ⚪ 很小 | 20min | 联调 |
| A12 | **分布式能力（全场景叙事缺口）** | 需多设备 | 🟠 叙事风险 | 1 天 | 双方 |

> **A1 + A2 是唯一真正阻塞答辩的两项。** 其余都可以在答辩前逐步补。

---

## A1 · ArkTS 编译全部前端改动（🔴 阻塞交付）

### 本轮改动过的前端文件（10 个）

| 文件 | 改动 |
|---|---|
| `api/AuthModels.ets` | 新增 3 个验证码请求/响应接口 + `VERIFICATION_CODE_MESSAGES` |
| `api/AgentApiClient.ets` | 新增 `authSendCode()` / `authVerifyCode()` + import |
| `api/AuthClient.ets` | 新增 `sendVerificationCode()` / `verifyCode()` + 错误码映射 |
| `api/AuthResult.ets` | 新增 `resendAfterSeconds` / `devCode` / `smsDelivered` |
| `api/AuthStore.ets` | token 加密落盘 + `encryptionDegraded()` + 异步 `initialize()` |
| **`api/TokenCipher.ets`** | **全新文件**（AES-256-GCM + 资产库） |
| **`utils/DeviceLayout.ets`** | **全新文件**（布局断点） |
| `entryability/EntryAbility.ets` | `DeviceLayout.attach()` + `AuthStore.initialize()` 改异步 |
| `pages/Login.ets` | 验证码 UI + 倒计时 + 响应式限宽 |
| `api/ApiModels.ets` / `api/ApiResponseValidator.ets` | 语义检索新字段 |
| `entry/src/test/DeviceLayout.test.ets` | **新增**（8 用例） |
| `entry/src/test/List.test.ets` | 注册新测试 |

**已删除**：`utils/Model3DRenderer.ets`（22,924 B，零引用死代码）
**已删除**：
> 删除前请团队确认一句：这个 3D 渲染器确实不再需要。
> 若要保留，从 git 历史或源前端包里取回即可。

### 要做什么

1. DevEco Studio 打开 `liantiao4/app`，Build → Build Hap(s)
2. **重点看这几处**（我无法编译，标出最可能出问题的）：
   - `TokenCipher.ets` 的 `cryptoFramework` / `asset` 用法（见 A3）
   - `AuthStore.initialize()` 由同步改**异步**后，`EntryAbility` 里的调用方式
   - `Login.ets` 的 `setInterval` / `clearInterval` 是否可用了对应类型
   - `DeviceLayout.ets` 的 `mainWindow.on('windowSizeChange', ...)` 回调签名
3. 目标：**0 ERROR**（上一轮基线是 0 ERROR / 90 WARN）
4. 归档新的 `build.log`

### 验收

- [ ] Build 成功，`build.log` 里 **0 ERROR**
- [ ] 新增的 4 个文件（`TokenCipher` / `DeviceLayout` / 2 个测试）**无编译错误**
- [ ] 归档 build.log

---

## A2 · 签名 → 真机安装 → 走通主链（🔴 阻塞答辩）

**现状**：`app/build-profile.json5` 的 `signingConfigs` 为**空数组**，
产出的是 `entry-default-unsigned.hap`。**未签名 HAP 连真机都装不上。**

### 步骤

1. `File > Project Structure > Signing Configs` → 勾 **Automatically generate signature**
   （需登录华为开发者账号）
2. DevEco Run 装机，或 `hdc install entry-default-signed.hap`
3. 按 `app/docs/前端回归检查清单_V1.0.md` 走主链，**本轮新增/改动处重点验**：

| # | 验证点 | 期望 |
|---|---|---|
| 1 | 登录页「验证码登录」标签 | 出现验证码输入框 + 「获取验证码」按钮（不是旧提示语） |
| 2 | 点「获取验证码」 | 出现倒计时；**若后端未配短信，必须显示「开发模式回显」而不是"已发送"** |
| 3 | 输错验证码 | 提示「验证码不正确，请检查后重试。」（不是原始英文） |
| 4 | 输对验证码 | 能进首页；新手机号自动建号 |
| 5 | 登录后**杀进程重开** | **仍是登录态**（验证 token 加解密往返正常） |
| 6 | 退出登录 | 回到登录页，且再重开仍是未登录 |
| 7 | 平板 / PC 上打开 | 登录页正文限宽居中，不拉伸 |
| 8 | 专注完成 | 首页完成度**不再是 0%** |
| 9 | 主动提醒 → 点通知 | 直达应用 |

### 验收

- [ ] `entry-default-signed.hap` 生成并成功装机
- [ ] 上表 9 项全部通过，归档截图
- [ ] 特别保留 #5 的**冷启动前后对比截图**（token 加密是否真的生效）

---

## A3 · `TokenCipher` 两处加密约定实测（🔴 会使登录态失效）

这是**风险最集中的一项**，单独列。

### 已确证的（有官方文档 + 本机 d.ts 双重依据）

| 用法 | 依据 |
|---|---|
| `createCipher('AES256\|GCM\|NoPadding')` | AES 一律三段式；GCM 属流模式，填充模式等价 |
| `Cipher.initSync(opMode, key, params)` **三参数** | `@ohos.security.cryptoFramework.d.ts:3551` |
| `createSymKeyGenerator('AES256')` | 密钥生成规格里 AES 只有"算法+长度"两段 |
| `convertKeySync({data: 32字节})` | d.ts `:2006`；AES256 必须正好 32 字节 |
| 加密后从 `doFinalSync` 输出**末尾取 16 字节**作 tag | 官方 GcmParamsSpec 表原文（4 个修订版一致） |
| **解密时 tag 只走 `spec.authTag`，不能拼进 data** | 官方 FAQ 把 key/iv/aad/tag/ciphertext 列为**5 个独立输入** |
| `asset.Tag.CONFLICT_RESOLUTION = ConflictResolution.OVERWRITE` | d.ts `:927` / `:1513`；默认 `THROW_ERROR`(`:941`) |
| `asset.Tag.RETURN_TYPE = ReturnType.ALL` | 不设它 `query` **不返回 SECRET** |

> 详细报告：`docs/20-token加密API核验报告.md`（含全部 d.ts 行号与文档原文）

### ⚠️ 仍然未实测的一点

**「`doFinalSync(明文)` 一次调用（不走 `update`）在所有情况下都返回 `密文‖tag`」**
—— 官方示例只演示了 `update + doFinal(null)`；"末尾 16 字节是 tag"来自
API 参考表格约定。本机无设备可实测。

**若要 100% 对齐官方示例**，把加密侧改成：

```typescript
cipher.initSync(cryptoFramework.CryptoMode.ENCRYPT_MODE, key, spec);
const cipherText: Uint8Array = cipher.updateSync({ data: plainBytes }).data;
const authTag: Uint8Array = cipher.doFinalSync(null).data;   // 此时恰好 16 字节，无需切片
```

这样不依赖"末 16 字节"的约定，两种情况都能跑。

### 兜底（已就位，不会把登录打挂）

`AuthStore.encodeToken()` 在加密失败时会**降级为明文**并把
`encryptionDegraded()` 置为 `true`，由账号页如实提示。
**即使上述约定有偏差，应用仍能正常登录**，只是退回改动前的存储强度。

### 要做什么

1. 真机上**登录一次 → 杀进程 → 重开**，确认仍是登录态
2. 若变成未登录：看 hilog 里 `TokenCipher` 的 warn
   （`crypto_init_failed:xxx` 或 `encrypt failed`）
3. 若确认是 tag 切片问题 → 按上面改成 `updateSync + doFinalSync(null)`
4. 确认无问题后，把 `TokenCipher.ets` 文件头里"仍未在本机验证的部分"一段删掉

### 验收

- [ ] 冷启动后仍为登录态
- [ ] hilog 里**无** `TokenCipher` 的 warn
- [ ] 归档冷启动前后截图 + hilog 片段

---

## A4 · 验证码登录端到端（真机）

后端已实现，前端刚接上，**但两边从未在真机上一起跑过**。

**验收**：

- [ ] 手机号 + 验证码可登录
- [ ] 新手机号自动注册并进入起始画像
- [ ] 60 秒内不能重复发送（按钮倒计时随后端返回值走）
- [ ] 8 个错误码都有准确中文提示：
      `RATE_LIMITED` / `DAILY_LIMIT_REACHED` / `PHONE_LOCKED` / `INVALID_CODE` /
      `CODE_EXPIRED` / `CODE_NOT_FOUND` / `SMS_NOT_CONFIGURED` / `SMS_PROVIDER_UNAVAILABLE`
- [ ] **未配短信时，界面明确显示"开发模式回显"而非"已发送"**

> 提示：`SMS_NOT_CONFIGURED` / `SMS_PROVIDER_UNAVAILABLE` 在默认环境下**必然触发**
> （后端没有 `SMS_PROVIDER`），正好用来验这两条文案。

---

## A5 · 14 个前端单测实际运行（🟠）

`app/entry/src/test/` 下现有 **14 个**测试文件（本轮新增 `DeviceLayout.test.ets`），
**全部从未运行过**。

**要做什么**：DevEco 里右键 `entry/src/test` → Run。

**验收**：

- [ ] 14 个测试文件全部通过
- [ ] 归档通过截图
- [ ] 若 `DeviceLayout.test.ets` 失败，优先看 `Number.isFinite` 是否可用
      （已改用 `x < 100000` 的写法规避，但仍需实测）

---

## A6 · 意图框架 `hasInsightIntent` 产物验证（🟠）

（详见 `docs/01-联调分析报告.md` §6，此处只列动作）

编译后检查产物，**不改源码**：

- [ ] `entry/build/.../module.json` 含 **`"hasInsightIntent": true`**
- [ ] `entry/build/.../resources/base/profile/insight_intent.json` 被重新生成为
      **`extractInsightIntents`** 结构（而非手写的 `insightIntentsSrcEntry`）
- [ ] `build.log` 里 **0 个 `InsightIntent Compiler Error`**
- [ ] 建议删除手写的 `resources/base/profile/insight_intent.json`（生成物，手写会混淆）
- [ ] 签名装机后对小艺说「开始学习」/「查看今日学习计划」，能拉起对应页面

---

## A7 · `DASHSCOPE_API_KEY` 配置（🔴 阻塞 AI 演示）

**现状**：`.env` **不存在**，`llm_ready=false`，全部 LLM 能力关闭
（聊天层走确定性规则兜底并诚实标注 `llmUsed=false`）。

**为什么我不配**：Key 是凭据，应由后端同学在自己机器上写入 `.env`，
**不该由联调负责人写死到工作区里**。

**要做什么**：

```powershell
cd E:\C4-liantiao\liantiao4\server\zhixue-agent-server
# 在 .env 里写（已被 .gitignore，不会进交付包）：
#   DASHSCOPE_API_KEY=sk-你的真实key
#   LLM_MODEL=qwen-plus
& E:\C4-liantiao\.venv-lt\Scripts\python.exe run.py --check
```

**验收**：

- [ ] `/api/agent/health` → `llm_ready: true`
- [ ] `POST /api/agent/chat` → `llmUsed: true`
- [ ] 前端 `ChatMain` 显示**蓝色**「大模型生成」而非橙色「本地规则兜底」
- [ ] 跑 `integration/run_liantiao4.py --llm-mode live` 并归档

> ⚠️ **顺手确认**：`chat_llm` 走 `qwen-vl-plus`（多模态识图需要），
> `qwen_adapter` 走 `qwen-plus`（工作流决策）。**两条路径的默认模型不同是刻意的**，
> 请在 `.env.example` 与 README 里写清楚，别把它们统一掉 —— 统一成 `qwen-plus` 会丢识图能力。

---

## A8 · 语义检索真实推理效果（🟠）

`app/agent/semantic.py` 的**融合逻辑**已用确定性假向量测过（23 项测试），
但**真实 embedding 的检索质量未验证**。

**要做什么**（配好 Key 后）：

1. 上传一份真实课程笔记到知识库
2. 问一个**字面不重合但语义相近**的问题，例如资料写"二叉树的后序遍历"，
   问"如何按左右根的顺序访问节点"
3. 对比配置 Key 前后的 `hits`

**验收**：

- [ ] `/knowledge-bases/{kbId}/search` 响应 `retrievalMode` = `hybrid:semantic+keyword+tag`
- [ ] `semanticAvailable: true` 且 `semanticWeight: 0.6`
- [ ] 上述语义查询能命中（纯关键词时命不中）—— **这是语义检索唯一有说服力的证据**
- [ ] 前端知识库页显示「检索方式：hybrid:semantic+keyword+tag」

---

## A9 · `@ohos.router` → `Navigation` 迁移（🟡 未做）

**61 处**废弃 `@ohos.router` 调用**没有迁移**。理由：

- `@ohos.router` 在 API 24 上**仍然可用**（只是标记废弃），不构成功能问题
- 迁移涉及 19 个页面的路由声明、参数传递、返回栈、`main_pages.json`
- **在无法编译验证的环境里做结构性路由改造，风险远大于收益**
  （一处没改对就是白屏/无法返回，且很难定位）

**建议分批**（不要一次全改）：

1. 先在**一个新页面**上用 `Navigation` + `NavPathStack` 建立范式
2. 再按"改动最少的页面"逐个迁移，每迁一个就真机验一遍返回栈
3. 最后统一去掉 `main_pages.json` 里已迁移的页面注册

---

## A10 · 其余 18 个页面的响应式落地（🟡 未做）

本轮只提供了能力（`utils/DeviceLayout.ets`）并示范了 `Login.ets`。
**完整改造牵动全部 19 个页面**，在无法编译验证的环境里风险过高。

**已在 `Login.ets` 建立的范式**（照抄即可）：

```typescript
Scroll() { ... }
  .constraintSize({ maxWidth: DeviceLayout.contentMaxWidth() })  // 大屏限宽，手机不约束
  .align(Alignment.Top)                                          // 居中
```

**建议顺序**：`Index`（首页）→ `ChatMain`（对话主界面）→ `StudyPlan` →
`KnowledgeBase` → 其余。

> ⚠️ 若时间不够，**替代方案是收紧 `deviceTypes` 只留 `phone`** ——
> 与其声明三种形态却只适配一种（答辩时会被追问），不如口径一致。
> 两种选择都能接受，**不要维持"声明三形态 + 零适配"的现状**。

---

## A11 · `414` 真实触发验证（⚪ 很小）

`test_client` **不施加请求行长度上限**，所以 414 只在真实 WSGI 服务器上才会触发。
目前 414 的处理器是"直接调用处理器"测的，**真实触发未覆盖**。

**要做什么**：加进 `integration/verify_integration.py`，对运行中的真实后端发一个
超长 URL（> 64KB 请求行）。

**为什么优先级低**：前端几乎不会发超长 URL。

---

## A12 · 分布式能力（🟠 叙事风险）

项目名是「基于 **HarmonyOS 全场景**的大学生主动助学 Agent」，而实测：

- 全前端搜 `distributed*` / 流转 / 接续 / 跨设备 → **只有 1 处命中，还是注释**
- `module.json5` **未申请任何分布式权限**
- 鸿蒙审计评分里「全场景分布式」**2.0/10**，是最低的一项

**建议二选一**（和队长对齐口径）：

- **A（推荐）**：做一个**最小可演示**的分布式点 —— 用 `distributedDataObject`
  把 `appState.activeFocusTask` 在手机与平板间同步，演示"手机上开始专注，
  平板上看到同一任务"。**一个点讲透 > 铺十个声明。**
- **B**：调整叙事，明确写"当前聚焦手机端单设备体验，多设备能力见路线图"

> ⚠️ **两种都行，但不能维持现状** —— 标题写全场景、内容零分布式，
> 是评委最容易追问的点。详见 `docs/13-全场景与华为账号登录方案.md`。

---

## 附：本轮"已完成且已验证"的部分

供对照，避免重复劳动（详见 `18-技术改进实施报告.md`）：

| 事项 | 验证方式 |
|---|---|
| 405/414/415/413 统一 JSON | 4 项单测 + 2 项联调 |
| CORS 收敛（保留真机局域网） | 4 项单测 + 2 项联调 |
| 对话历史按用户隔离 | 7 项单测 + 2 项联调 |
| SQLite 可选后端 + 迁移工具 | 24 个用例 + 双后端各跑一次联调 |
| 知识库语义检索 + 诚实降级 | 23 项单测 + 3 项联调 |
| 联调不再污染交付包 | 实测变化 0 B |
| 演示基线可恢复 | 324,009 → 38,418 B |
| `loop.py` 误判回退（字节码逐指令一致） | 6 项单测 + 基线未漂 |
| 前端死代码清理 | 静态分析确认零引用 |
