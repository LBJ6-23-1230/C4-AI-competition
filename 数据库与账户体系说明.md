# 数据库接入与账户体系说明

> 本文回答两个问题：**数据存在哪**、**账户登录怎么保证正确**。
> 所有结论均为实测，命令可直接复跑。

---

## 一、数据存储：两套后端，同一个抽象

项目**没有引入外部数据库服务**，而是通过 `Repository` 抽象提供两套实现，
由环境变量切换。业务代码一行都不用改。

| | **JSON**（默认） | **SQLite**（可选） |
|---|---|---|
| 文件 | `data/repository.json` | `data/repository.sqlite` |
| 优点 | **可直接肉眼查看**，答辩时能当场打开给评委看 | 每次只写一行，并发由数据库保证 |
| 适合 | 演示、单机、人工核对 | 数据量增长、多进程部署 |
| 写入 | 每次 `save()` **全量重写整个文件** | 只写一行 |
| 并发 | 进程内 `RLock`，**跨不了进程** | WAL + `busy_timeout`，数据库级保证 |

### 实测差异（来自 `tests/test_sqlite_backend.py`）

```
已存储记录      JSON 单次 save     SQLite 单次 save
200 条          1640.9 ms          13.1 ms        ≈ 125 倍
```

并发写同一文件时，JSON 版双进程失败率 **5–19%**（进程内锁跨不了进程）。

> **为什么默认仍是 JSON**：演示场景下"能当场打开文件核对数据"比写入速度更重要。
> 数据量增长或多进程时再切 SQLite —— 这是**刻意的取舍**，不是没做。

---

## 二、怎么切到 SQLite

### 2.1 一条命令迁移

```bash
python tools/migrate_repository.py --dry-run   # 先看会迁什么
python tools/migrate_repository.py --verify    # 正式迁移 + 逐条比对
```

实测输出：

```
  集合: 7 个，记录 21 条
  已写入 21 条 -> data/repository.sqlite   （24,576 B）
  比对完成：21 条，全部一致
```

**迁移只读源文件** —— JSON 不会被删改，随时可切回。

### 2.2 用 SQLite 启动

```powershell
$env:ZHIXUE_DB="sqlite"; python run.py
```

支持 `sqlite:<path>` 指定库文件位置；取值非法会直接报错并列出可选项。

### 2.3 数据表结构

刻意做成**通用三元组**，而不是每个集合一张表：

```sql
CREATE TABLE records (
    collection TEXT NOT NULL,
    item_id    TEXT NOT NULL,
    value      TEXT NOT NULL,       -- JSON 序列化的记录体
    PRIMARY KEY (collection, item_id)
);
CREATE INDEX idx_records_collection ON records(collection);
```

**为什么这样设计**：两层接口（`/api/agent/*` 与 `/api/v1/**`）新增集合时
**不需要改 schema**，与既有的 `Repository` 抽象保持一致 ——
加一个集合就是多一个 `collection` 取值，不动表结构。

实测库内集合（联调跑完后的 scratch 库）：

```
evidences 4 · plan_histories 3 · plans 3 · profiles 3 · sessions 4
submissions 4 · traces 6 · users 2 · workflows 3      总计 32 条
```

---

## 三、SQLite 后端的验证

| 验证项 | 结果 |
|---|---|
| 后端单元测试 | **327 passed** |
| SQLite 专项测试 | **24 passed**（`tests/test_sqlite_backend.py`） |
| **全量接口联调** | **95 / 95（100%）** —— 用 `ZHIXUE_DB=sqlite` 实跑 |
| 演示基线 | **零漂移**：66.67 / 全错 0.0 / 42→58 / [30,30]→[45,15] |
| 产物证明 | scratch 目录生成的是 `integration-5400.sqlite`（不是 JSON） |

> 联调脚本会设 `ZHIXUE_REPOSITORY` 指向临时 JSON 路径；
> `create_app` 在 `ZHIXUE_DB=sqlite` 时用 `path.with_suffix(".sqlite")` 派生库文件，
> 所以**确实走的是 SQLite**，不是被覆盖成 JSON。

---

## 四、迁移时踩过的两个坑（已修，记录备查）

### 4.1 每方法新开连接 → 慢几个数量级

最初每个方法都 `sqlite3.connect()` 新开连接，并在 `_connect()` 里执行
`PRAGMA journal_mode=WAL`。后果是**每次读写都要切换 journal 模式并 fsync**，
基准测试（1280 次 save）**超过 120 秒**都没跑完 —— 比 JSON 版还慢。

现在改为：**单个长连接 + 一次性初始化 PRAGMA + 可重入锁串行化**。

### 4.2 缺 `ids()` / `clear()` → 切后端就 500

`demo/reset` 做"按归属删除"需要主键列表，`auth` 的会话清理需要清空集合。
`SQLiteRepository` 一开始没实现这两个方法，**切到 SQLite 后直接 AttributeError → 500**。
补上后与 `JsonRepository` 同名同义。

---

## 五、账户体系：数据存在哪、怎么隔离

### 5.1 账户相关集合

| 集合 | 内容 |
|---|---|
| `users` | 账号主体（`userId` / `nickname` / `phone` / `grade` / `authProvider` / `createdAt`） |
| `sessions` | 会话 token（有效期 **30 天**） |
| `verification_codes` | 手机验证码（`expiresInSeconds: 300`，重发间隔 60 秒） |

### 5.2 跨账号数据隔离（实测）

这是账户体系里最要紧的一条 —— **实测确认隔离有效**：

| 场景 | 结果 |
|---|---|
| 甲建知识库 | HTTP 201 |
| 乙列出自己的知识库 | 看不到甲的（返回空） |
| **乙按 id 直接访问甲的库** | **HTTP 404**（不是 403 也不是 200） |
| 甲列出自己的知识库 | 能看到自己的 |

实现方式：知识库记录都带 `userId`，`_get_owned()` 按归属校验，
非本人一律按"不存在"处理（不泄露资源是否存在）。

### 5.3 鉴权边界

| 情况 | 行为 |
|---|---|
| 不带 token 访问 `/auth/me` | **401** |
| 伪造 token | **401** |
| 注销后再用旧 token | **401**（立即失效） |

### 5.4 手机号与验证码

* **手机号必填**：注册缺手机号 → 400「手机号不能为空」；同号重复 → **409**
* **验证码是模拟的**：`smsDelivered: false` + 返回 `devCode`
  —— 刻意如此：开发/演示模式返回开发码，**生产模式拒绝暴露**
* 错误验证码 → 400 `CODE_NOT_FOUND`

---

## 六、🔴 一个真实缺陷：登录后知识库报契约不一致

### 现象

前端同学报：**游客能进知识库，登录自己账号后进不去**，
顶部红条报「服务返回的数据结构与 api-contract-v0.3 不一致」。

### 根因

`AgentApiClient.listKnowledgeBases()` 在登录后会拼上 userId：

```
/api/v1/knowledge-bases?userId=xxx
```

而 `ApiResponseValidator` 当时对路径用**精确比较**：

```ets
if (path === '/api/v1/knowledge-bases') {   // 带 ?userId= 时不成立
```

带查询串时该分支不成立 → **掉进「详情」分支**去要 `kbId`/`name`
→ 列表响应没有这两个字段 → 校验失败 → 报契约不一致。

**这正是"游客可以、账户登录就出 bug"的原因** —— 游客不带 userId。

### 修复

`ApiResponseValidator` 新增 `basePath()`，进入 `isValid` 即剥离查询串，
类内**全部 33 处**路径匹配由 `path` 改为 `route`。

**并新增回归闸门** `tools/check_validator_query_string.py`：
校验器内部不得对裸 `path` 做匹配；客户端里真正拼查询串的函数其路由必须能被
`route` 命中。**双向验证过**：修复后 PASS，改回旧写法 FAIL。

---

## 七、账户全链路闸门（可复跑）

新增 `tools/check_auth_chain.py`，覆盖 **22 项**：

```bash
python tools/check_auth_chain.py                  # JSON 后端
python tools/check_auth_chain.py --port 5500      # SQLite 后端
```

| 分组 | 检查项 |
|---|---|
| 注册 | 201 / 返回 token / 返回 userId / hasPhone=true |
| 参数校验 | 同号重复 409 / 缺手机号 400 |
| 验证码 | 发码 200 含 devCode / 正确码通过 / 错误码被拒 |
| Token 鉴权 | 有效 token 通过 / 无 token 401 / 伪造 token 401 |
| **跨账号隔离** | 列表互不可见 / 按 id 直取被拒 / 本人可见 |
| 昵称登录 | 已注册昵称可用 / 未注册昵称 404 |
| 注销 | 200 / 注销后 token 失效 |

**实测结果（两个后端都跑）**：

```
JSON   后端（5000）：22 项通过 22，失败 0
SQLite 后端（5500）：22 项通过 22，失败 0
```

> 工具**可反复运行**：昵称带随机后缀。后端把「一个昵称对应多个账号」
> 视为 409（提示改用更精确的昵称）—— 这是正确行为，所以探针不能用固定昵称。

---

## 八、怎么做才能看到"数据真的写进去了"

这是评委和队友最常问的。三种方式：

| 方式 | 怎么做 |
|---|---|
| **JSON 后端（默认）** ⭐ | 直接打开 `data/repository.json`，肉眼可见 `profiles` / `plans` / `knowledge_bases` 等集合 |
| **SQLite 后端** | `python -c "import sqlite3;c=sqlite3.connect('data/repository.sqlite');print(c.execute('SELECT collection,COUNT(*) FROM records GROUP BY collection').fetchall())"` |
| **重置数据** | `POST /api/v1/demo/reset`（或删掉文件让后端首启重建） |

> ⚠️ `repository.json` / `repository.sqlite` 都是**运行期状态**，
> 已在 `.gitignore` 中 —— **绝不入库、绝不进交付包**。

---

## 九、当前演示环境用的是哪个

**JSON**（默认）。原因：演示时要能当场打开文件给评委核对数据。

SQLite 已经**完整可用并验证过**（327 单测 + 24 专项 + 95/95 联调），
属于"随时可切、切了也不会坏"的可选能力，而不是未验证的声明。

---

*本文所有数字均来自实跑输出，命令可直接复现。*
