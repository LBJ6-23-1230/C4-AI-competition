# 前端提交指南（分支 `main2`）

> **给前端同学**
> 分支已建好：`main2`（从 `main` 分出，含全部最新修复）

---

## 一、为什么要用单独分支

`main` 是**提交快照** —— 仓库根放着已经打包好的提交物（签名 HAP、提交 zip）。
如果直接在 `main` 上开发并全量推送，会覆盖这些提交物。

所以：

| 分支 | 用途 | 谁能推 |
|---|---|---|
| `main2` | **你的开发分支** | ✅ 你 |
| `main` | 提交快照（已冻结的交付物） | 队长 |

---

## 二、第一步：切到 main2

```powershell
git clone https://github.com/LBJ6-23-1230/C4-AI-competition.git
cd C4-AI-competition
git checkout main2
```

已经 clone 过的：

```powershell
git fetch origin
git checkout main2
git pull origin main2
```

---

## 三、⚠️ 不要改 / 不要覆盖这些文件

它们是已经打包好的**提交物**，改了会破坏提交版本：

| 路径 | 说明 |
|---|---|
| `知学Mate+暗影骑士王们.zip` | **要上传的提交压缩包** |
| `hap/知学Mate-signed.hap` | 签名 HAP（已验签，与 AGC 指纹对齐） |
| `01-作品说明文档/` | 必交材料 |
| `02-演示视频/` | 必交材料 + 录制指导 Word |
| `03-演示文件与源代码/` | 开发工作区的原样副本 |
| `04-制作辅助文档/` | PPT 参考 Word |
| `项目文档/` `实证材料/` | 28 + 46 份文档与证据 |
| 根目录的几份 `*.md` 说明 | 提交/交接/答疑文档 |

### 你要改的是这些（源码）

| 路径 | 内容 |
|---|---|
| `app/` | **HarmonyOS ArkTS 前端 —— 你的主战场** |
| `server/` | Python 后端（一般不用你动） |
| `contracts/` `app/contracts/` | 接口契约（**要改必须两边同步**，见第五节） |
| `integration/` `tools/` | 联调与检查脚本 |

---

## 四、提交前先同步 main 的最新修复

`main` 上可能有比你本地更新的修复。**推之前先拉一次**：

```powershell
git fetch origin
git merge origin/main        # 或 git rebase origin/main
```

### 目前 main 上已有的关键修复（别重复改）

| 修复 | 说明 |
|---|---|
| **知识库页契约报错** | `listKnowledgeBases` 带 `?userId=` 时校验器走错分支 —— 已修（`ApiResponseValidator.basePath()`） |
| **搭子页接入后端算分** | 联机模式不再显示"暂不支持"，改为真实五因子匹配 |
| 雷达图三处死代码 | `chartTarget`/`chartWeak`/`chartStrong` 原先从未赋值，已接通 |
| 两个对话页图标 | 语音改话筒、拍照改照相机 |
| 大屏布局 | 登录页 `constraintSize` 位置修正 |

> 如果你本地还是旧代码，**这些修复会被你的推送覆盖掉** —— 所以务必先 `merge`。

---

## 五、改契约时要两边同步

接口契约是**逐字节一致**的双份：

```
contracts/openapi.json          ← 真源
app/contracts/openapi.json      ← 镜像（必须与真源逐字节相同）
```

只改一份会让闸门红。改完自查：

```powershell
python tools/diff_contracts.py
# 期望：结论 PASS（真源与镜像逐字节一致）
```

---

## 六、推送前自查（建议都跑一遍）

```powershell
# ① ArkTS 能否编译（最关键）
#    DevEco Studio 里 Build > Build Hap(s)，或命令行：
#    见 README 的构建命令

# ② 契约是否漂移
python tools/diff_contracts.py

# ③ 前端调用是否都有校验器分支（防止再出"知识库"那类问题）
python tools/check_frontend_validator_coverage.py

# ④ 带查询串的调用会不会让校验器走错分支
python tools/check_validator_query_string.py

# ⑤ 有没有把运行态数据混进来
python tools/check_runtime_data.py .
```

---

## 七、不要提交这些

`.gitignore` 已经挡住大部分，但手工确认一下：

| 不要提交 | 原因 |
|---|---|
| `.env` | 含 API Key |
| `data/repository.json` | 运行期状态，首启自动重建 |
| `data/*.sqlite*` | 同上（SQLite 后端） |
| `chat/history.json` | 运行期对话记录 |
| `app/entry/build/`、`.hvigor/`、`oh_modules/` | 构建产物与依赖，体积大且每次变 |
| `app/local.properties` | 本机 SDK 路径 |
| `app/build-profile.json5` 里的签名路径改动 | 应始终保持**相对路径** `../zhixue-signing/...`；改成 `E:/`、`D:/` 这类绝对路径会让别人拉下来就构建失败 |

> 签名材料怎么配见根目录的 **`签名材料交接说明.md`**。

---

## 八、推送

```powershell
git add -A
git status                    # 确认只改了该改的
git commit -m "feat(frontend): 说明这次改了什么"
git push origin main2
```

推完告诉队长，由队长合并到 `main`。

---

## 九、如果推错了 / 想撤销

**不要自己 `push --force`** —— 全量强推会覆盖提交物。

先跟队长说，让队长处理。你本地可以用：

```powershell
git reset --soft HEAD~1        # 撤销最近一次 commit，改动仍在工作区
git restore --staged <文件>     # 把某文件移出暂存区
```

---

## 十、一句话总结

> 在 `main2` 上改 `app/` 下的源码；
> **推之前先 `git merge origin/main`**（拿最新修复）；
> 跑一遍第六节的自查；
> **别碰根目录的提交物**（HAP / zip / 各份说明文档）。

---

*本文由项目维护。分支与修复清单同步自 `main` 的当前状态。*
