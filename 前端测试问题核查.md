# 前端测试问题逐条核查

> 针对前端同学在真机/模拟器实测中提出的 5 个问题，逐条查证源码与实测接口的结果。
> 每条都标注结论、证据、以及**是否需要改**。

---

## 汇总

| # | 问题 | 结论 | 要改吗 |
|---|---|---|---|
| 1 | 知识库页报「服务返回的数据结构与 api-contract-v0.3 不一致」 | **真 bug**（已修） | ✅ 已修 |
| 2 | 搭子页显示「联机模式暂不生成搭子结果」 | **不是 bug** —— 前端从未实现该接口，提示是诚实的 | ⚠️ 可选增强 |
| 3 | 「接口环境」页到用户那里该删掉 | **是内部入口，但本作品是演示场景，建议保留** | ❌ 建议保留 |
| 4 | 登录注册真的能用吗 | **能用**（下面有实测证据） | ❌ |
| 5 | 数据库在哪能看 | **JSON 文件，不是数据库** | ❌ |

---

## 1. 知识库页契约报错 —— 真 bug，已修

### 现象

进「我的知识库」页 → 顶部红条报
**「服务返回的数据结构与 api-contract-v0.3 不一致」** → 无法建知识库。

### 我一开始的判断是错的

我先逐个实测了后端全部知识库端点，**全部 200、结构完全符合校验器要求**：

```
GET  /api/v1/knowledge-bases              → {knowledgeBases, total}          ✅
GET  /api/v1/knowledge-bases/<id>         → {kbId, name, courseName, ...}    ✅
GET  /api/v1/knowledge-bases/<id>/documents → {kbId, documents, total}       ✅
POST /api/v1/knowledge-bases/<id>/search  → {kbId, query, hits, ...}         ✅
```

所以我当时判断"后端没问题"。**但那个判断不完整** —— 没考虑 URL 带查询串的情况。

### 真正的根因

`AgentApiClient.listKnowledgeBases()`（第 109-114 行）会拼上 userId：

```ets
const suffix: string = userId !== undefined && userId.length > 0
  ? `?userId=${encodeURIComponent(userId)}` : '';
return AgentApiClient.request(..., `/api/v1/knowledge-bases${suffix}`);
```

而校验器里对路径用的是**精确比较**：

```ets
if (path === '/api/v1/knowledge-bases') {   // ← 带 ?userId= 时不成立
```

于是带查询串时这个分支不成立，**掉进下面的「详情」分支**去要 `kbId` / `name`，
而列表响应里没有这两个字段 → 校验失败 → 页面报契约不一致。

### 为什么"游客能进、她进不去"

| 身份 | 是否带 userId | URL | 结果 |
|---|---|---|---|
| 游客 / demo-user | 不带 | `/api/v1/knowledge-bases` | 精确比较成立 → **正常** |
| **登录真实账号** | 带 | `/api/v1/knowledge-bases?userId=xxx` | 精确比较失败 → **报错** |

这正好对应她说的「**我这是用我自己的账号进去的**」。

### 修复

在 `ApiResponseValidator` 里新增 `basePath()`，函数一进来就把查询串剥掉：

```ets
private static basePath(path: string): string {
  const mark: number = path.indexOf('?');
  return mark >= 0 ? path.substring(0, mark) : path;
}

static isValid(method: string, path: string, payload: Object): boolean {
  ...
  // 一律用去查询串的路径做匹配，避免 `?a=b` 让精确比较失效。
  const route: string = ApiResponseValidator.basePath(path);
```

然后把类内**全部 33 处**路径匹配从 `path` 改为 `route`。

> 全类统一改，而不是只补知识库那一条 —— 否则以后任何新接口一带参数就会重演。

### 验证

* ArkTS 全量构建 **BUILD SUCCESSFUL**，`ApiResponseValidator` 无 ERROR
* 新增闸门 `tools/check_validator_query_string.py`：
  - 修复后 **PASS**
  - 反向测试（把 `route` 改回 `path`）**FAIL exit 1** —— 证明真能拦住

---

## 2. 搭子页「联机模式暂不生成搭子结果」 —— 不是 bug

### 后端明明支持，为什么页面说不支持？

**因为前端从来没实现这个接口。** 核对结果：

| 环节 | 状态 |
|---|---|
| 后端路由 `POST /api/v1/agent/partner-match` | ✅ 存在 |
| 实测响应 | ✅ HTTP 200，返回 2 个候选、匹配到「小红」，`realModeUnavailable: false` |
| 联调脚本覆盖 | ✅ 98 项之一（`verify_integration.py:612`） |
| 前端客户端方法（`AgentApiClient`） | ❌ **没有** |
| 前端响应模型（`ApiModels`） | ❌ **没有** |
| 前端校验器分支 | ❌ **没有** |

所以页面那段话：

> 「当前后端尚未提供学习搭子匹配接口。为避免把本地规则结果伪装成在线 Agent 结果，本页不会展示虚构候选人。」

**是诚实且准确的**（严格说应改成"前端尚未接入"，但意思对）——
它宁可显示"没接入"，也不拿本地假数据糊弄。这符合项目的诚实降级原则。

### 一个相关代码问题

`PartnerMatch.ets:25` 有一行：

```ets
this.realModeUnavailable = !AgentApiClient.getConfig().useFixture;
```

字面上是"非离线模式 → 标记为不可用"。**但在目前"前端没接接口"的前提下，这个逻辑是对的**
（联机时确实拿不到数据）。等真要接入时，这一行必须一起改掉，否则会出现
"接好了接口却还显示不可用"。

### 要不要现在做

后端能力**已就绪**，前端补齐需要：客户端方法 + 响应模型 + 校验器分支 + 页面渲染（约 4 处改动）。

**建议**：如果演示视频里想展示"学习搭子"，就做；否则保持现状也可以，
页面提示是诚实的，不扣分。**演示时不要在联机模式下点进这一页**，观感不好。

---

## 3. 「接口环境」页该不该给用户看到

### 这句判断是对的

它确实是**内部联调工具**（切 Fixture / 改后端地址 / 自检），不是面向用户的功能。
产品视角下，它的入口应该只在开发版出现。

### 但本作品的情况不同

这是**参赛演示作品**，而且有三条硬需求依赖它：

| 依赖点 | 说明 |
|---|---|
| 演示视频录制 | 需要现场切换离线/在线，展示"诚实降级"这个技术点 |
| 评委自己装了 HAP | 需自行填后端地址（模拟器 `10.0.2.2` / 真机局域网 IP） |
| 诚实边界说明 | 这是"配置透明"的体现，答辩时可以主动讲 |

**所以建议保留**，但可以做两件事改善观感：

1. 把入口**从首页显眼位置挪到「我的 → 设置」**里（降低用户误入）
2. 页面文案已经写了「切换后会清除短期接口缓存，不会修改后端、项目代码或课程数据」
   —— 这句可以保留，它交代了副作用

> 如果你坚持要"产品化"，可以加一个编译期开关（debug 版才显示入口）。
> 但那会让评委装到的 release HAP 失去地址配置能力 —— **不建议**。

---

## 4. 登录注册能用吗 —— 能用

### 实测证据

```
POST /api/v1/auth/register   {nickname, phone, grade}
  → HTTP 201
  → {"token":"ZZ1BKah6...","expiresAt":"2026-10-23T15:32:32+00:00",
     "user":{"authProvider":"local","grade":"大二","hasPhone":true,...}}

POST /api/v1/auth/send-code  {phone}
  → HTTP 200
  → {"devCode":"144065","expiresInSeconds":300,
     "phoneMasked":"139****3504","resendAfterSeconds":60,"smsDelivered":false}
```

### 要理解的行为

| 点 | 说明 |
|---|---|
| **手机号必填** | 注册接口要求 phone；不传或重复号码会 400 / 409 |
| **验证码是模拟的** | `smsDelivered: false` + 返回 `devCode` —— **没有接真实短信服务商**，这是刻意的：开发/演示模式返回开发码，生产模式拒绝暴露 |
| **登录方式** | 昵称免密登录（演示用）、手机验证码登录、华为账号一键登录（需真机） |
| **Token 有效期** | 30 天（`expiresAt`） |

### ⚠️ 一个必须知道的安全边界

后端 `login-with-huawei` **不校验 OpenID 真伪**（那需要服务端调华为接口验签）。
演示与单机场景可用；**生产环境必须补验签**，否则伪造 `openId` 即可登入他人账号。
这一点已如实写进项目文档，答辩时可以主动说明。

---

## 5. 数据库在哪 —— 没有数据库，是 JSON 文件

### 位置

```
server/zhixue-agent-server/data/
├── question_bank.json    题库（35 题，内容数据，随包提供）
└── repository.json       **运行期状态（相当于"数据库"）**
```

### 怎么看

`repository.json` 就是一个普通 JSON，记事本/VSCode 直接打开即可。
当前内容：

```
profiles          1 条    ← 学习画像（含掌握度）
plans             1 条    ← 学习计划
traces            1 条    ← Agent 决策轨迹
knowledge_bases   2 条    ← 知识库
```

### 为什么用 JSON 而不是数据库

**刻意的选择**，理由是**可核对**：

- 答辩时能**当场打开文件**给评委看数据真的写进去了
- 联调脚本可以断言"三份数据文件前后逐字节不变"（验证没有意外写脏数据）
- 零部署：评委 clone 下来不用装数据库就能跑

> 多进程部署时可切 SQLite：设环境变量 `ZHIXUE_DB=sqlite` 即可
> （`app/repositories/sqlite_repository.py` 已实现，接口与 JSON 版一致）。

### ⚠️ 注意

`repository.json` 是**运行期状态**，已在 `.gitignore` 中 ——
**绝不入库、绝不进交付包**。首启会自动重建。

> 想重置数据：`POST /api/v1/demo/reset`，或直接删掉该文件让后端重建。

---

## 附：她提到的其它两点

### 「头像昵称什么的都没有」

她说的是「我的」页（截图里显示的是一个数字 `1` 和"独立账号 · 数据归你所有"）。

这里确实可以做得更像产品：显示**头像 + 昵称 + 手机号（脱敏）+ 注册时间**。
`GET /api/v1/auth/me` 已返回这些字段（`nickname` / `phone` / `createdAt` / `grade`），
**后端数据齐全，是前端没渲染**。改动量小，建议做 —— 演示时"我的"页是常被点开的。

### 「AI 的测试根本不能相信」

这句要分情况理解，值得当面澄清：

- **如果指"AI 写的代码不能信"** —— 同意。我们的做法是每个结论都留证据：
  342 项单元测试、98/98 接口联调、契约逐字节一致闸门、三轮独立代码审计（34 项发现）。
  **测试同学发现的问题就是最有效的验证**（这次 5 个问题里有 2 个是真问题）。
- **如果指"AI 出的题目不能信"** —— 这也正是**项目刻意不让模型出题**的原因：
  出题与判分全部由确定性代码完成，同一输入永远同一结果。

---

## 待确认

以上第 2、3 项涉及**产品决策**（搭子要不要接、接口环境入口怎么放），
需要队长确定后我再动手。第 1 项已修复并验证。

---

*本文所有结论均实测核对，标注了文件与行号。*
