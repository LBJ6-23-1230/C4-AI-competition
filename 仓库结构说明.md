# 本仓库结构说明

> **本仓库（`main` 分支）就是最终提交版本 V2。**
> 仓库根目录 = 提交版结构，clone 下来即可直接浏览、构建、运行。

---

## 一、顶层目录各自是什么

| 路径 | 是什么 | 提交时用不用 |
|---|---|---|
| `知学Mate+暗影骑士王们.zip` | **打包好的提交压缩包**（含签名 HAP + 全量源码 + 作品说明文档） | ✅ **这才是要上传的** |
| `hap/知学Mate-signed.hap` | 签名 HAP 的可读副本（release，有效至 2029-09-21） | 加分材料 |
| `01-作品说明文档/` | 必交材料（含官方模板与改稿须知） | ✅ 必交 |
| `02-演示视频/` | 必交材料（含**视频录制指导 Word**：整体流程 + 逐镜旁白；视频待录） | ✅ 必交 |
| `04-制作辅助文档/` | **PPT 制作参考 Word**（项目参考文本 + 18 页设计架构 + 答辩追问准备） | 辅助材料 |
| `app/` | HarmonyOS ArkTS 前端工程（DevEco 可直接打开） | — |
| `server/` | Python Agent 后端（可直接 `python run.py`） | — |
| `contracts/` | 接口契约真源（`api-contract-v0.3`） | — |
| `integration/` | 一键联调脚本（契约 + 单测 + 95 项 HTTP） | — |
| `tools/` | 构建、契约校验、布局检查、运行态检查等脚本 | — |
| `03-演示文件与源代码/` | 开发工作区里源码的**原样副本**（含 `联调与验证/`） | 不参与打包 |
| `项目文档/` | 27 份技术文档（审计报告、分布式报告、真机说明等） | — |
| `实证材料/` | 42 份实跑证据（联调汇总、界面截图、大屏布局证据等） | — |
| `README-提交说明.md` | 怎么跑、验证数据、要上传什么 | — |
| `提交清单与待办.md` | 逐项状态与行动表 | — |
| `后端启动与部署指南.md` | **后端完整启动/配置/排障手册**（12 节，可直接作为技术文档提交） | — |

### 为什么 `03-演示文件与源代码/` 与根目录的 `app/ server/ ...` 内容重复？

* **根目录那份**：为了让评审在线点开就能看到标准工程结构 ——
  DevEco 要求"打开含 `build-profile.json5` 的目录"，摊平后 `app/` 即符合；
  `hap/` 也在根目录，可直接下载。
* **`03-.../` 那份**：开发机上 V2 工作区的原样保留 ——
  那份工作区里源码在编号目录下（与 `01-` `02-` 并列），保持一致便于人工核对。

两份内容**完全相同**（构建脚本会逐目录校验数量一致）。

---

## 二、开发历史在哪

本仓库的 `main` 是**提交快照**（单一提交）。
完整的 33 个提交的开发历史（含每一轮修复与验证记录）在
**`dev-history` 分支**上，供追溯用：

```bash
git log dev-history --oneline      # 33 个提交，从 liantiao5 首版到最终提交
```

`main` 与 `dev-history` 的**文件内容一致**，差别只在提交历史。

> `backend` / `frontend` 两个分支是更早期的历史留档，**不要使用** ——
> 它们是前后端分离时代的版本，`frontend` 分支还带 ArkUI-X 插件
> （会导致 DevEco Sync 失败），且不含登录、验证码、华为账号、服务卡片。

---

## 三、怎么跑起来

见 [README-提交说明.md](README-提交说明.md) 的 §四。

最短路径：

```bash
cd server/zhixue-agent-server
pip install -r requirements.txt
cp .env.example .env          # 填 DASHSCOPE_API_KEY（可选，不填会诚实降级）
python run.py                 # 监听 0.0.0.0:5000
```

前端用 DevEco Studio 打开 `app/`，取消勾选「自动生成签名文件」后运行 `entry`。

App 内「工具 → 接口环境」改后端地址：模拟器用 `http://10.0.2.2:5000`，
真机用 `http://<开发机局域网IP>:5000`。

---

## 四、当前验证状态

| 项 | 结果 |
|---|---|
| 后端单元测试 | 327 passed |
| 全量接口联调 | 95 / 95（100%） |
| 契约一致性 | 真源与镜像逐字节一致（109,269 B，同 sha256） |
| ArkTS 编译 | 全量重编 0 编译错误，产物 2.25 MB |
| 签名 HAP | `verify-app success`，AGC 指纹已对齐 |
| 安装运行 | 模拟器 `install bundle successfully` |
| 演示基线 | 零漂移（66.67 / 全错 0.0 / 42→58 / [30,30]→[45,15]） |
| 全场景分布式 | 双端 `joined session` 实测建立 |
| 代码审计 | 三轮独立审计 34 项，已修 28 项（含 5 个 P0） |

**未验证项**（需 HarmonyOS NEXT 真机，本团队不具备）：
华为账号一键登录、小艺口令、分布式跨设备数据流动。
原因与如实表述见 `项目文档/29-真机测试不必要性说明.md`。

---

*2026 中国高校计算机大赛 · 人工智能创意赛 · 鸿蒙赛道 ｜ 参赛队伍：暗影骑士王们*
