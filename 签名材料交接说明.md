# 签名材料交接说明

> **给要用这套签名构建 App 的同学**
> 适用场景：在 DevEco Studio 里 Build / 装到模拟器或真机运行

---

## 一、结论先讲

**可以继续用原来这套签名，而且必须继续用。**

交接时要注意三点（都是实测踩出来的）：

1. 要拿的不止三个文件，而是**三份文件 + 一个 `material/` 目录**（见第三节）；
2. 签名路径是**相对路径**，材料要放在**仓库里的 `zhixue-signing/`**，不再要求 E 盘（见第四节）；
3. **换一台电脑就必须在 DevEco 里重填一次口令** —— 仓库里 `build-profile.json5`
   存的是加密串，与本机绑定，在别人的机器上**解不开**（见第五节）。

---

## 二、为什么必须用同一套签名（不是"能用就用"）

华为账号一键登录、小艺（意图框架）、应用升级，这三项都和**签名证书指纹**绑定：

| 环节 | 绑定关系 |
|---|---|
| 华为账号一键登录 | 华为在 AGC 校验**签名证书指纹**，不匹配就报 `1001500001` |
| 小艺 Agent / 意图框架 | 与 AGC 侧的 **appid** 绑定 |
| 应用升级覆盖安装 | 签名不一致会**装不上**（覆盖失败） |

我们的签名指纹已经登记在 AGC 里（两条：公钥指纹 + 证书指纹）。
**如果换一套签名，这些授权会全部失效。**

> 另外：**不要勾选「自动生成签名文件」**。自动签名会让 appid 随机化，
> 同样会打断上述授权关系。

---

## 三、要交接的材料（三份文件 + 一个目录）

| 文件 | 作用 | 必需 |
|---|---|---|
| `zhixue-debug.p12` | **私钥 + 证书**（真正用来签名的东西） | ✅ 必需 |
| `zhixue-release-profileRelease.p7b` | **发布 Profile**（AGC 签发，绑定包名与证书） | ✅ 必需 |
| `zhixue-release.cer` | **证书链**（3 张：叶子 / 中间 CA / 根 CA） | ✅ 必需 |

> ⚠️ **`.hap` 不能代替它们。** HAP 里只有签名结果，**没有私钥** ——
> 拿 HAP 可以安装运行，但**不能用来给新代码重新签名**。
> 所以"只发我打好的 HAP"这条路走不通，必须给上面三个文件。

我们的签名目录里还有两个文件，**不是必需的**：

| 文件 | 说明 |
|---|---|
| `zhixue-debug-cert.cer` | 调试证书，**项目配置里没引用**它 |
| `zhixue.csr` | 证书签名请求，**项目配置里没引用**它 |

### ⚠️ 还有第四个东西：`material/` 目录（别漏）

要交接的其实**不止三个文件**，还有一个 `material/` **目录**：

```
zhixue-signing\
├── zhixue-debug.p12
├── zhixue-release-profileRelease.p7b
├── zhixue-release.cer
└── material\                    ← ⚠️ 别漏
    ├── ac\<hash>
    ├── ce\<hash>
    └── fd\{0,1,2}
```

hvigor 在签名时会去**证书同目录**下找 `material/`。实测漏掉它会直接报：

```
ENOENT: no such file or directory, stat '<仓库>\zhixue-signing\material'
```

> 2026-09-26 实测补记：原文档只列了三个文件，按它做会卡在这一步
> —— 报错信息里只有一行 `Error Code: 00308018 Unknown Error`，
> 不带 `ENOENT` 的话很难看出是在找目录。**整个 `zhixue-signing\` 目录一起拷过去最省事。**

---

## 四、放到哪里（这一步错了就构不出来）

项目的 `app/build-profile.json5` 里签名路径是**相对路径**：

```json5
"storeFile": "../zhixue-signing/zhixue-debug.p12",
"profile":   "../zhixue-signing/zhixue-release-profileRelease.p7b",
"certpath":  "../zhixue-signing/zhixue-release.cer"
```

`..` 是相对于 `app/` 目录，也就是**仓库根目录**。所以把整个 `zhixue-signing\` 目录
（含 `material\`）放到仓库根目录即可：

```
<仓库根目录>\zhixue-signing\
├── zhixue-debug.p12
├── zhixue-release-profileRelease.p7b
├── zhixue-release.cer
└── material\                   ← 别忘了这个（见第三节）
```

就不挑盘符了。**但要留意下面这个「两份副本」的坑。**

### ⚠️ 仓库里有**两份** `app/`，相对路径各算各的

```
<仓库根>/app/                       →  ../zhixue-signing/ = <仓库根>/zhixue-signing/
<仓库根>/03-演示文件与源代码/app/    →  ../zhixue-signing/ = <仓库根>/03-演示文件与源代码/zhixue-signing/
```

两边看的是**两个不同的目录**。不管从哪一份构建，材料都要放到那一份旁边的
`zhixue-signing/` 里 —— 根目录放了一份不代表 `03-` 那份能找到。
（`03-演示文件与源代码/` 是交付副本，一般不用它构建；但真要用就得自己放一份。）

> ✅ **2026-09-26 变更说明**：这份配置原先写的是 `E:/zhixue-signing/...` 这个**写死的绝对路径**，
> 等于要求所有人必须在 `E:` 盘建同名目录。实测有队友把材料放在
> `D:\project\zhixue_pro\zhixue-signing\`，于是"拉取最新代码就构建失败"，
> 报找不到 `storeFile` / `profile` / `certpath` —— **不是 GitHub 限制，也不是必须放 E 盘**，
> 而是那条绝对路径只对原作者那台机器成立。现在改成相对路径，材料跟着仓库走。
>
> ⚠️ **`zhixue-signing/` 已在 `.gitignore` 里**（第 17 行），所以这三个文件**不会**被提交，
> 拉取代码也不会自动得到它们 —— 这正是下面第五节要手动放的原因。
>
> ⚠️ **请不要再把绝对路径提交回仓库**。那会把"只有我能构建"的问题重新带回来。

---

## 五、DevEco Studio 里的操作

1. 用 DevEco Studio 打开 **`app/` 目录**（不是只打开 `entry/`）
2. 等 **Sync** 完成
3. 打开 `File > Project Structure > Signing Configs`
4. 确认：
   - 「**自动生成签名文件**」**未勾选** ← 关键
   - `Store File` → 指向 `zhixue-debug.p12`
   - `Profile File` → 指向 `...profileRelease.p7b`
   - `Certpath File` → 指向 `zhixue-release.cer`
   - `Key Alias` = `zhixue-debug`
   - `Sign Alg` = `SHA256withECDSA`
5. 点 OK → 构建 `entry` 模块

### 密码要不要填？—— **换机器就必须重填**

> ⚠️ **2026-09-26 更正：** 本节原先写的是"通常不用，DevEco 能直接用加密值"。
> **实测这是错的** —— 那条只对**生成这份配置的那台电脑**成立。

`build-profile.json5` 里的 `storePassword` / `keyPassword` 是形如
`0000001C…` 的 **DevEco 加密串，加密密钥与本机绑定**。换一台电脑后：

* 即使把 `material/` 目录也一起拷过去，hvigor 仍然报
  `Incorrect keystore password, please input the correct plaintext password.`；
* 直接把明文写进去也不行 —— hvigor 要求该字段 ≥32 位：
  `00303116 Configuration Error: The length of the storePassword or keyPassword field is less than 32`，
  并提示"清空 signingConfigs，去 Project Structure → Signing Configs 重新应用签名材料"。

**所以换机器后必须走一次第五节的步骤 3–5，在弹出的框里输入明文口令。**
DevEco 会用它重新加密写回 `build-profile.json5`，并在 `zhixue-signing/`
下重建属于你这台机器的 `material/`。

> **原始口令请向队长索取，不要写在文档、群或仓库里。**
> （本仓库这份文档刻意不记口令，也不该记。）

#### 不想用 DevEco GUI 的替代路径

命令行 `hvigorw assembleHap` **一定会卡在 `SignHap`**（加密串解不开），
但它在失败前已经产出了 `entry-default-unsigned.hap`。可以直接调签名工具手工签：

```bash
# 1) 构建（SignHap 会失败，正常）
hvigorw --mode module -p product=default -p buildMode=debug assembleHap --no-daemon

# 2) 手工签名（用明文口令，不经过 DevEco 的加密）
java -jar "<DevEco>/sdk/default/openharmony/toolchains/lib/hap-sign-tool.jar" sign-app \
  -mode localSign -keyAlias zhixue-debug \
  -keyPwd <明文口令> -keystorePwd <明文口令> \
  -appCertFile zhixue-signing/zhixue-release.cer \
  -profileFile zhixue-signing/zhixue-release-profileRelease.p7b \
  -inFile entry-default-unsigned.hap -outFile entry-default-signed.hap \
  -keystoreFile zhixue-signing/zhixue-debug.p12 \
  -signAlg SHA256withECDSA -compatibleVersion 60101024

# 3) 校验 + 安装
java -jar "<DevEco>/…/hap-sign-tool.jar" verify-app -inFile entry-default-signed.hap \
  -outCertChain out.cer -outProfile out.p7b     # 期望 "Digest verify result: true"
hdc install -r entry-default-signed.hap
```

> 2026-09-26 在**全新 clone** 上实测走通：`CompileArkTS` 一次过，
> 手工签名成功、`verify-app` 返回 `true`、`hdc install` 装上模拟器。

---

## 六、⚠️ 不要把密码提交到仓库

`app/build-profile.json5` 里的密码字段是加密串，本身就**不该公开**。
提交前务必确认：

```bash
git diff app/build-profile.json5     # 确认没有把个人路径改动提交上去
```

判断标准：三行路径都以 **`../zhixue-signing/`** 开头。
若看到 `E:/`、`D:/`、`C:/` 这类盘符，说明有人把它改回了绝对路径 ——
**不要提交**，那会让别人拉下来就构建失败（见第四节）。

---

## 七、验证签名是否生效

构建成功后，产物在：

```
app/entry/build/default/outputs/default/entry-default-signed.hap
```

- 若文件名是 `entry-default-**unsigned**.hap` → 签名**没进去**，回第 5 步检查
- `entry-default-signed.hap` ≈ **2.26 MB**（含约 54 KB 签名块）

用 DevEco 自带工具独立验签（可选，命令行）：

```powershell
& 'D:\DevEco\DevEco Studio\jbr\bin\java.exe' -jar `
  'D:\DevEco\DevEco Studio\sdk\default\openharmony\toolchains\lib\hap-sign-tool.jar' `
  verify-app -inFile 'app\entry\build\default\outputs\default\entry-default-signed.hap' `
  -outCertChain 'chain.cer' -outProfile 'profile.p7b'
```

期望看到：

```
Find Hap Signing Block success, version: 3, block count: 3
profile type is: release
verify codesign success
verify: Verify success
verify-app success
```

> ⚠️ 该工具**只接受 `-inFile` / `-outCertChain` / `-outProfile` 三个参数**，
> 多传 `-outProof` 会 exit=1 且不产出文件。

---

## 八、常见报错

| 报错 | 原因 | 解决 |
|---|---|---|
| `ENOENT: … stat '<仓库>\zhixue-signing\material'` | **漏了 `material/` 目录**（原文档只说三个文件） | 把整个 `zhixue-signing\` 拷过去，含 `material\`（第三节） |
| `Incorrect keystore password, please input the correct plaintext password` | **换了电脑**，加密口令与本机绑定、解不开 | 在 DevEco 的 Signing Configs 里重填明文口令（第五节） |
| `00303116 Configuration Error: The length of the storePassword or keyPassword field is less than 32` | 有人把**明文口令**直接写进了 `build-profile.json5`（hvigor 只认加密串） | 清空 `signingConfigs`，到 Project Structure → Signing Configs 重新应用；**不要把明文提交** |
| `Failed :entry:default@SignHap` + `parseAlgParameters failed: ObjectIdentifier()` | 口令不对，**或**命令行构建时命中了 **JDK 8**（PATH 里旧 JDK 排在前面） | 先按第五节重填口令；若仍失败再把 DevEco 的 JBR 提到 PATH 最前：<br>`$env:PATH = "D:\DevEco\DevEco Studio\jbr\bin;$env:PATH"` |
| 找不到 `storeFile` / `profile` / `certpath` | 签名材料没放到相对路径算出来的位置 | 按第四节放到**那一份** `app/` 旁边的 `zhixue-signing\`（注意有两份 `app/`） |
| 生成的是 `entry-default-unsigned.hap` | 「自动生成签名文件」被勾上了，或签名配置没选中 | 取消勾选；确认 `products[0].signingConfig` 是 `default` |
| 华为账号登录报 `1001500001` | 签名证书指纹与 AGC 登记不一致 | 说明用的不是这套签名 —— 换回本套 |
| DevEco Sync 失败 | 用了旧的 `frontend` 分支（带 ArkUI-X 插件） | 用 `main` 分支 |

---

## 九、一句话交接

> 把 **`zhixue-signing` 整个文件夹**（含 `material\`）发给我；
> 我放到**仓库根目录**下的 `zhixue-signing\`。
> DevEco 打开 `app/` → 不勾选「自动生成签名文件」→ 在 Signing Configs 里
> 选中那三个文件并**输入明文口令**（换机器必须填，配置里那串加密值只对原机器有效）。

---

*本文由项目维护。路径与字段值核对自 `app/build-profile.json5`；
`material/` 与口令跨机器的问题为 2026-09-26 实机复现后补记
（在全新 clone 上验证：`CompileArkTS` 通过、手工签名 + `verify-app` 通过、`hdc install` 成功）。*
